from dataclasses import dataclass


@dataclass
class Strings:
    """
    should contain the 25 password strings shifted by one letter each to later be decrypted into the original password
    """
    strings = (
        "",
        "",
    )
